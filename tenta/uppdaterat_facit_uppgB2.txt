import math

def getangle(p):
    rad = math.atan2(p.pos[1], p.pos[0])
    if rad < 0:
        rad += 2 * math.pi
    return rad

# plist består av fyra pkt (1,0), (0,1), (0,-1), (-1,0)
plist = [ Point(1,0), Point(0,1), Point(0,-1), Point(-1,0) ]

# Sortera plist med avseende på funktionen getangle
plist2 = sorted(plist, key=getangle)